#include <Tails/Managers/Input.hpp>

namespace tails
{
    void InputManager::added(EngineSubsystem& engineSubsystem)
    {
        // load inputs from json via engine's paths variables
    }

    void InputManager::tick(float deltaTime)
    {
        // tick contexts
    }
}
